[install]
linker = "isolated" # Prevent hoisting from resolving `path-scurry@2` to `lru-cache@6` (missing `LRUCache`), which breaks the `@velix/desktop` postinstall native rebuild.
exact = true
minimumReleaseAge = 259200
